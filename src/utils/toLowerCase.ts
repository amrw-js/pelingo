/**
 * Returns the given string in lower case.
 * If no argument is given, an empty string is returned.
 * @param {string} [string=''] String to be converted to lower case
 * @returns {string} The lower case string
 */

export const toLowerCase = (string: string = '') => {
  return string.toLowerCase()
}
