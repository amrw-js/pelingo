import LanguageInputs from '../components/LanguageInputs/LanguageInputs'

// import { useGeminiTranslator } from '../hooks/useGeminiTranslator'

const Header = () => {
  // const { translate, translating } = useGeminiTranslator()

  return (
    <div>
      <LanguageInputs />
    </div>
  )
}

export default Header
